<%= form_with(model: simple, local: true) do |form| %>
  <% if simple.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(simple.errors.count, "error") %> prohibited this simple from being saved:</h2>

      <ul>
      <% simple.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
